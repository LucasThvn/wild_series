{% extends 'base.html.twig' %}

{% block body %}
    <section>
            <h1><a href="{{ path('wild_show', { 'slug': program.title|replace({' ':'-'})|lower }) }}">{{ program.title }}</a></h1>
            <h2>Épisode {{ episode.number }} : {{ episode.title }}</h2>
            <h3><a href="{{ path('season_show', {'id' : season.id}) }}">Saison {{ season.number }}</a></h3>
            <p>{{ episode.synopsis }}</p>
    </section>
{% endblock %}